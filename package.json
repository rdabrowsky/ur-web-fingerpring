{
  "name": "browser-fingerprint-matrix",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "deploy": "npm run build && gh-pages -d dist",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "latest"
  }
}
